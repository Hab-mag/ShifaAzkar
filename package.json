{
  "name": "shifa-azkar",
  "version": "1.0.0",
  "description": "Шифа — Аяты, Азкары и Дуа (офлайн)",
  "scripts": {
    "build": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "capacitor": {
    "android": {
      "allowMixedContent": true
    }
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
